#!/usr/bin/env bash

FILE="/sys/devices/pci0000:00/0000:00:1f.0/PNP0C09:00/VPC2004:00/conservation_mode"

case "$1" in
    1|0) echo "$1" | sudo tee "$FILE" > /dev/null ;;
    "")  cat "$FILE" ;;
    *)   echo "Usage: $0 [0|1]" ;;
esac
